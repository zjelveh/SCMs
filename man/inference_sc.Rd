% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference_sc.R
\name{inference_sc}
\alias{inference_sc}
\title{Perform Inference for Synthetic Control Method}
\usage{
inference_sc(
  sc.pred,
  dataset,
  inference_type = "scpi",
  P = NULL,
  u.missp = TRUE,
  u.sigma = "HC1",
  u.order = 1,
  u.lags = 0,
  u.design = NULL,
  u.alpha = 0.05,
  e.method = "all",
  e.order = 1,
  e.lags = 0,
  e.design = NULL,
  e.alpha = 0.05,
  sims = 200,
  rho = NULL,
  rho.max = 0.2,
  lgapp = "generalized",
  cores = 1,
  w.bounds = NULL,
  e.bounds = NULL,
  verbose = TRUE
)
}
\arguments{
\item{sc.pred}{List. Results from the synthetic control estimation.}

\item{dataset}{Data frame. The original dataset used for synthetic control estimation.}

\item{inference_type}{Character. Type of inference to perform: "scpi" or "placebo". Default is "scpi".}

\item{P}{Matrix. Prediction matrix (not used in the current implementation).}

\item{u.missp}{Logical. Whether to account for model misspecification. Default is TRUE.}

\item{u.sigma}{Character. Type of heteroskedasticity-consistent standard errors. Default is "HC1".}

\item{u.order}{Numeric. Order of the misspecification. Default is 1.}

\item{u.lags}{Numeric. Number of lags for misspecification. Default is 0.}

\item{u.design}{Matrix. Design matrix for misspecification (optional).}

\item{u.alpha}{Numeric. Significance level for uncertainty intervals. Default is 0.05.}

\item{e.method}{Character. Method for estimating errors. Default is "all".}

\item{e.order}{Numeric. Order of the error process. Default is 1.}

\item{e.lags}{Numeric. Number of lags for error process. Default is 0.}

\item{e.design}{Matrix. Design matrix for error process (optional).}

\item{e.alpha}{Numeric. Significance level for error intervals. Default is 0.05.}

\item{sims}{Numeric. Number of simulations for inference. Default is 200.}

\item{rho}{Numeric. Correlation parameter (optional).}

\item{rho.max}{Numeric. Maximum correlation. Default is 0.2.}

\item{lgapp}{Character. Type of lag polynomial approximation. Default is "generalized".}

\item{cores}{Numeric. Number of cores to use for parallel processing. Default is 1.}

\item{w.bounds}{List. Bounds for weights (optional).}

\item{e.bounds}{List. Bounds for errors (optional).}

\item{verbose}{Logical. Whether to print progress information. Default is TRUE.}
}
\value{
A list containing the inference results, either from SCPI or placebo methods.
}
\description{
This function performs inference for synthetic control estimates using either SCPI (Synthetic Control Prediction Intervals) or placebo methods.
}
\examples{
# Example usage (replace with actual example when available)
# inference_results <- inference_sc(sc_results, my_data, inference_type = "scpi")
}
