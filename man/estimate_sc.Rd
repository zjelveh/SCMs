% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_sc.R
\name{estimate_sc}
\alias{estimate_sc}
\title{Estimate Synthetic Control Model}
\usage{
estimate_sc(
  dataset,
  outcome,
  covagg,
  col_name_unit_name,
  name_treated_unit,
  col_name_period,
  treated_period,
  min_period,
  end_period,
  outcome_models = c("none", "ols", "ridge", "lasso", "augsynth"),
  feature_weights = "uniform",
  w.constr = NULL,
  V = "separate",
  V.mat = NULL,
  solver = "ECOS",
  constant = FALSE
)
}
\arguments{
\item{dataset}{A data frame containing the panel data.}

\item{outcome}{Character. Name of the outcome variable (column in dataset).}

\item{covagg}{List of covariate-aggregation operations passed to
\code{\link{scdata}}. Each operation should define at least \code{var}, and can
optionally define \code{select_periods}, \code{partition_periods}, and
\code{compute}.}

\item{col_name_unit_name}{Character. Column name in dataset containing unit names (e.g., state names).}

\item{name_treated_unit}{Character. Name of the treated unit (e.g., the state that received treatment).}

\item{col_name_period}{Character. Column name in dataset containing time periods.}

\item{treated_period}{Numeric. Time period when treatment starts for the treated unit.}

\item{min_period}{Numeric. Earliest time period in the dataset.}

\item{end_period}{Numeric. Latest time period in the dataset.}

\item{outcome_models}{Character vector. Outcome models to fit. Default is c("none", "ols", "ridge", "lasso", "augsynth").}

\item{feature_weights}{Character. Method for assigning weights to predictors ("uniform" or "optimize"). Default is "uniform".}

\item{w.constr}{Optional. Constraints on the weights.}

\item{V}{Character. Covariance matrix estimation method. Default is "separate".}

\item{V.mat}{Optional. Pre-computed covariance matrix.}

\item{solver}{Character. Solver to use for the optimization problem. Default is "ECOS".}

\item{constant}{Logical. Whether to include a constant term in synthetic control weights. Default is FALSE.}
}
\value{
A list of class \code{"scm_estimate"} containing estimated SCM results
and input parameters.
}
\description{
This function processes and estimates a Synthetic Control Model (SCM) based on the provided dataset and parameters.
}
\examples{
\donttest{
toy <- data.frame(
  unit = rep(c("treated", "donor1", "donor2"), each = 6),
  year = rep(1:6, 3),
  y = c(10, 11, 12, 14, 15, 16,
        9, 10, 11, 12, 13, 14,
        11, 12, 13, 13, 14, 15)
)
fit <- estimate_sc(
  dataset = toy,
  outcome = "y",
  covagg = list(
    list(var = "outcome_var", partition_periods = list(type = "by_period"))
  ),
  col_name_unit_name = "unit",
  name_treated_unit = "treated",
  col_name_period = "year",
  treated_period = 5,
  min_period = 1,
  end_period = 6,
  outcome_models = "none"
)
names(fit)
}
}
