% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_scm_dataset.R
\name{create_scm_dataset}
\alias{create_scm_dataset}
\title{Create Dataset for Synthetic Control Method}
\usage{
create_scm_dataset(
  dataset,
  outcome,
  covagg,
  col_name_unit_name,
  name_treated_unit,
  col_name_period,
  treated_period,
  min_period,
  end_period,
  constant = FALSE
)
}
\arguments{
\item{dataset}{Data frame or data.table containing the panel data.}

\item{outcome}{Character. Name of the outcome variable column.}

\item{covagg}{List of covariate-aggregation operations passed through to
\code{\link{scdata}}.}

\item{col_name_unit_name}{Character. Name of the column containing unit names.}

\item{name_treated_unit}{Character. Name of the treated unit.}

\item{col_name_period}{Character. Name of the column containing time periods.}

\item{treated_period}{Numeric. Time period when treatment starts.}

\item{min_period}{Numeric or NULL. Earliest time period to include. If NULL, uses the minimum in the dataset.}

\item{end_period}{Numeric or NULL. Latest time period to include. If NULL, uses the maximum in the dataset.}

\item{constant}{Logical. Whether to include a constant term. Default is FALSE.}
}
\value{
A list containing the prepared data for SCM analysis.
}
\description{
This function prepares a dataset for use with the Synthetic Control Method (SCM) by formatting and standardizing the input data.
}
\examples{
# Example usage (replace with actual example when available)
# scm_data <- create_scm_dataset(dataset = my_data, outcome = "gdp", 
#                                covagg = list(
#                                  list(var = "outcome_var", partition_periods = list(type = "by_period")),
#                                  list(var = "population", compute = "mean"),
#                                  list(var = "education", compute = "mean")
#                                ),
#                                col_name_unit_name = "state", name_treated_unit = "California",
#                                col_name_period = "year", treated_period = 2000,
#                                min_period = 1990, end_period = 2010)
}
